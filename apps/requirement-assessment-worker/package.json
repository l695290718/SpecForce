{
  "name": "@specforge/requirement-assessment-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/repository.ts",
  "types": "src/repository.ts",
  "scripts": {
    "dev": "tsx src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@specforge/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
