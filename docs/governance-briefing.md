# Governance Briefing / 治理简报

Generated 2026-08-26T09:50:59.456Z from SpecForge system records via MCP. Digest `64d822ac6622ab9f`. Regenerate with `pnpm governance:briefing` - do not hand-edit.

基于 SpecForge 系统记录经 MCP 于 2026-08-26T09:50:59.456Z 生成。摘要 `64d822ac6622ab9f`。使用 `pnpm governance:briefing` 重新生成——请勿手改。

## Orientation checklist / 入职清单
1. Run or read docs/governance-briefing.md before your first implementation turn.
2. Open a design-context preflight in the exact owning scope before changing code, schema, or contracts.
3. Write every authored fact through MCP tools; never bypass to direct database access.
4. Keep English canonical fields complete and provide full Chinese overlays for human-facing content.
5. Close the session with CONVERGED evidence only after focused tests, build, and live verification pass.
6. Record deferred work as backlog facts with owner, trigger, and rationale - never silently drop it.

1. 首轮实现前运行或阅读 docs/governance-briefing.md。
2. 修改代码、模式或契约前，在精确归属 Scope 打开设计上下文预检。
3. 所有已编写事实必须经 MCP 工具写入；禁止绕行直查或直写数据库。
4. 英文字段保持规范完整，面向人的内容提供完整中文覆盖。
5. 聚焦测试、构建与线上验证全部通过后，才能以 CONVERGED 证据关闭会话。
6. 延期工作必须登记为带负责人、触发条件与理由的待办事实——不得静默丢弃。

## Business rules in force (12) / 生效业务规则
- **rule-bilingual-asset-completeness** — Bilingual asset content must be complete: Every MCP-persisted design asset that enters governance snapshots must include complete canonical English content and complete semantically aligned Chinese localized content.
- **rule-policyhub-scope-isolation** — Application Service Scope Isolation: Normal reads must remain inside one application service.
- **rule-specforge-3a-projection-publication** — 3A Projection Publication Rule: Publishes a 3A projection only after an exact-Scope leased build proves endpoint closure, deterministic counts, compatible Profile/schema, and canonical content digest.
- **rule-specforge-agent-bootstrap-fail-closed** — Agent Bootstrap Fail-Closed Rule: Blocks non-trivial work unless the Agent has current exact-Scope governance context, a valid session, converged design state, and enforceable attestation coverage.
- **rule-specforge-assessment-acceptance** — SpecForge Assessment Acceptance Rule: Controls whether an assessment can be accepted for implementation handoff based on exact Scope, evidence, reconciliation, confidence, and review state.
- **rule-specforge-core-service-reuse** — Web and MCP must reuse Core Service: Web UI and MCP Server must call packages/core for design asset operations and must not duplicate business logic.
- **rule-specforge-knowledge-promotion-transaction** — Atomic Knowledge Promotion Transaction: Promotes approved candidates into canonical assets, typed links, Evidence, outbox events, a ChangeSet, and one immutable receipt atomically.
- **rule-specforge-knowledge-risk-policy** — Knowledge Candidate Risk Policy: Assigns T0-T3 review risk and enforces actor separation, evidence, localization, and human escalation before promotion.
- **rule-specforge-mcp-write-audit** — MCP write tools must be audited: Every MCP tool call, especially write operations, must record an AuditLog entry with actor, action, target, input summary, output summary, and status.
- **rule-specforge-nebula-bounded-generation-publication** — Nebula Bounded Generation Publication Rule: Publishes or rolls back a graph generation only through an atomic PostgreSQL head-pointer transaction after exact-Scope parity and compatibility gates pass.
- **rule-specforge-relationships-required** — Design assets require explicit relationships: Related design assets must be connected with typed AssetLink records so graph queries and future impact analysis can traverse dependencies.
- **rule-specforge-seed-through-mcp** — Seed writes must call MCP tools: Database seed/import routines must call MCP write tools instead of directly calling Prisma upserts for design asset data.

## Accepted ADRs (35) / 已接受架构决策
- **adr-3a-architecture-navigation-workspace** — 3A Architecture Navigation Workspace: SpecForge can derive deterministic Business, System, and Technical Architecture projections from governed Knowledge Baselines, but the Web console does not expose a Baseline-bound 3A browser. The generic asset graph uses design-asset identities and current relationships, so it cannot safely represent historical Knowledge Assertions, Profile-bound projections, or official Baseline-to-Baseline drift.
- **adr-agent-driven-legacy-baseline-discovery** — Agent-Driven Legacy Baseline Discovery: SpecForge must initialize design knowledge for existing enterprise application services without asking each product team to deploy another daemon. Coding Agents such as Claude Code and OpenCode can already reach source workspaces, but Agent-only reading is nondeterministic, expensive, difficult to resume, and weakly auditable. A fixed scanner is repeatable but cannot recover undocumented business intent by itself. The enterprise hierarchy and token grants already exist, and one token may authorize several application services, while every write must target one exact application-service Scope.
- **adr-agent-governance-briefing** — Agent Governance Briefing and the Orientation Path: A generated bilingual governance briefing compiled from system records through MCP: pnpm governance:briefing writes docs/governance-briefing.md (digest-stamped), the same core compiler powers the live /governance/briefing surface, and agent catalog reads go through MCP instead of direct database access. AGENTS.md synchronization from the record is registered as a backlog fact.
- **adr-application-service-scope-isolation** — Application-Service Scope Isolation: SpecForge exposes design assets through Web views and MCP tools. An implicit default or a module-level context can leak assets between application services and makes authorization inconsistent. The initial owned service is `com.huawei.celon.desiner`; every authored asset, proposal, context pack, and relationship must carry that exact application-service identity and materialized path.
- **adr-architecture-overview-home** — Architecture Overview Home: SpecForge needs a system-orientation entry point that explains its MCP-first design-fact lifecycle without turning the application root into an operational dashboard. Application-service scopes are an isolation boundary: authored design facts, derived counts, and dashboard metrics belong to one selected application service and must not be read or aggregated merely because a visitor opens the root route.
- **adr-asset-to-3a-mapping-semantics** — Asset-to-3A mapping semantics: Make design-asset-to-3A assignment the primary mapping contract and separate unit-to-unit architecture realization.
- **adr-bounded-nebula-knowledge-projection-generations** — Bounded Knowledge-Assertion-Aware Nebula Projection Generations: The current NebulaGraph runtime projects exact-Scope design-asset identities and current typed relationships from authoritative PostgreSQL events. It does not yet preserve the Baseline-bound Knowledge Assertions, Profile-pinned projection identity, versioned relationship snapshot, or publication semantics of the PostgreSQL-first 3A model. Unlimited immutable graph generations would provide history but would multiply online storage, writes, indexes, cache pressure, and cleanup cost.
- **adr-canonical-english-localized-overlay** — English Canonical, Bilingual Overlays: Agents and integrations need stable English keys and predictable semantics, while people in the owning Huawei service need Chinese-readable decision content. A single translated field would make canonical comparisons unstable; English-only records would make human review incomplete.
- **adr-continuous-observation-governance** — Continuous Observation Governance: Phase 2 can derive deterministic BIZ, SYS, and TECH views from a published Baseline. Phase 3 needs a durable receiving boundary for Agents and future live connectors. A connector must be able to resume after a process or network failure without silently dropping, reordering, or duplicating observations. The receiving boundary must remain exact-Scope and must not turn an observation into an authoritative design fact.
- **adr-cross-scope-integration-contracts** — Governed Cross-Scope Integration Contracts and a Bounded Integration Atlas: Huawei internal landscapes contain many application services that call each other through APIs, events, and files. SpecForge already uses one application-service scope per service (`com.huawei.celon.desiner`, `com.huawei.celon.integrationgateway`, `com.huawei.celon.specstudio`, and `com.huawei.celon.policyhub`), scope grants through `readableScopes`, and an `IntegrationContract` asset type. A call has a consumer-side business fact, but provider details can be protected by a different Scope. The previous design left four production risks open: one Designer session was incorrectly treated as sufficient for all consumer writes; contract identity did not bind a specific target operation or revision; read authorization could disclose provider details indirectly; and Atlas size limits had no executable query semantics.
- **adr-delta-connector-run-completion** — Delta connector run completion: Close successful DELTA connector runs atomically without invoking snapshot deletion inference.
- **adr-design-context-preflight-gate** — Design Context Preflight Gate: The existing governance policy requires MCP synchronization before completion, but it does not yet require an Agent to read the current design facts before editing code. This allows an implementation to drift before its matching ADR, Proposal, Context Pack, relationships, and evidence are updated.
- **adr-design-fact-dual-record-governance** — Design-Fact Dual-Record Governance: SpecForge exposes architectural decisions and design facts to both reviewers in Git and agents through MCP. A repository-only ADR is reviewable but not queryable by the design center; an MCP-only record is queryable but lacks the immutable change history and ordinary code-review workflow. Missing scope, localization, links, or evidence also makes a fact unsafe to use for impact analysis and context generation.
- **adr-deterministic-3a-knowledge-projections** — Deterministic 3A Knowledge Projections: Phase 1 can publish an immutable, reviewed Baseline, but the Baseline is not yet useful as a coordinated business, system, and technical architecture view. Phase 2 needs reproducible 3A projections without turning derived views into a second authoring store. The projection must also explain cross-layer alignment, compare a current accepted set with a pinned Baseline, and give Agents a stable Context Pack.
- **adr-enterprise-3a-semantic-coverage** — Enterprise 3A semantic coverage projection: Preserve the reviewed v6 architecture baseline and materialize an immutable exact-Scope coverage projection for every authored record.
- **adr-expressive-web-design-system** — Expressive Mission-Control Web Design System: The SpecForge web shell was functional but visually plain: flat white cards, hairline borders, a light sidebar, and no expressive hierarchy between navigation, heroes, and content. Users asked for a cooler, more distinctive overall style ("酷炫一点") without sacrificing readability, accessibility, or the exact-Scope information design. Prior increments already introduced motion utilities (`sf-scan`, `sf-rise`, `sf-border-live`) and the dark overview canvas, but tokens were minimal and the shell stayed light-only.
- **adr-federated-design-fact-synchronization** — Federated Design-Fact Synchronization: SpecForge needs one governed way to represent externally observed implementation facts without granting external systems authority over approved design intent. The first delivery must establish exact Scope isolation, source-neutral federation records, policy-driven candidate promotion, durable delivery/audit behavior, and read-only reconciliation while avoiding unproven claims about scanners or external mutation.
- **adr-integration-verification-states** — Integration Contract Verification States From Attestation Evidence: Extends VALIDATES to integration contracts so ordinary evidence assets authored via MCP become per-contract attestations; the bounded atlas derives ATTESTED/DRIFT/BLOCKED_ATTESTATION/UNATTESTED by latest recordedAt and shows them everywhere; first real attestation covers the legacy MCP stdio contract from this session's receipts.
- **adr-local-git-hook-change-attestation** — Local Git Hook Change Attestation Gate: SpecForge currently provides an MCP design-context preflight and evidence-backed Design Change Session closure, but other users and coding Agents can still modify repositories without reading design assets. Repository instructions are cooperative rather than enforceable. The first distributable control must work across language stacks and prevent normal local commits that lack exact-Scope design coverage and convergence, while preserving CodeHub protected-branch enforcement as a later production control.
- **adr-mcp-first-architecture** — MCP-First Design Authoring: Design authoring must have one auditable write path. Direct database writes or Web-only mutation paths can bypass scope authorization, localization validation, audit events, and relationship checks. MCP provides the shared boundary used by human workflows and agents.
- **adr-nebulagraph-derived-impact-runtime** — NebulaGraph as the Derived Impact Runtime: Impact analysis needs bounded, explainable traversal over typed design relationships. Local development and automated tests need a deterministic and inexpensive runtime, while enterprise deployments may need a distributed graph engine for higher graph volume and query concurrency. The runtime must preserve exact scope authorization and must never make a derived projection an authoring authority.
- **adr-nebulagraph-production-projection** — NebulaGraph Production Projection: Enterprise impact analysis requires multi-hop traversal over a relationship graph that may grow beyond the practical operating envelope of recursive PostgreSQL queries. The graph runtime must scale independently without becoming an authoring authority or weakening application-service isolation.
- **adr-parallel-governance-workstreams** — Parallel Governance Workstreams: SpecForge already has exact application-service Scope isolation, MCP preflight/closure, a local Go Git Hook, and signed Change Attestations. Production adoption needs a stronger identity boundary and a repository-authoritative CI verification contract, but CodeHub/CodeArts and enterprise identity providers are external integrations that must not be implied by local code.
- **adr-postgresql-authoritative-design-store** — PostgreSQL as the Authoritative Design Store: SpecForge must preserve authored design facts, relationship history, auditability, and reproducible derived views. MCP writes need one transactional authority that can enforce exact application-service scope and support local development, automated tests, and small installations. A graph query engine may optimize traversal, but it must not become an independent authoring source.
- **adr-provider-neutral-agent-bootstrap-governance** — Provider-Neutral Agent Bootstrap and Layered Governance Context: SpecForge already provides exact-Scope `prepare_design_change` and `close_design_change_session` operations, a standalone Go CLI, `.specforge.yaml` repository mappings, and a signed local change-attestation gate. The repository also contains `AGENTS.md` and provider-specific examples. These pieces do not guarantee that Claude Code, OpenCode, Codex, or a future enterprise Agent discovers the same governance policy and relevant design intent when it opens a repository. Instruction files are cooperative, vendor-specific, and easy to omit or copy out of date. Returning the entire Scope catalog would also waste tokens and make prompt-injection and stale-context risks worse.
- **adr-readable-3a-architecture-mapping** — Readable 3A Architecture Mapping: The existing 3A Overview is a bounded relationship network rather than a readable architecture map. In the observed Designer Scope, 151 nodes and 234 edges occupy a 454-by-576-pixel canvas. Suppressing labels protects rendering density but prevents a reader from identifying the business capability, system realization, and supporting technology chain.
- **adr-readable-3a-architecture-mapping-v6** — 3A semantic unit expansion v6: The readable 3A view needs service-level semantic units for the selected SpecForge capabilities.
- **adr-requirement-assessment-center** — Evidence-Driven Requirement Assessment Center: SpecForge stores scoped design assets, typed relationships, 3A mappings, ownership, and reconciliation evidence. Those facts become materially useful when a requirement can be evaluated against a reproducible, authorized catalog slice. Asset counts or unconstrained model prompts cannot establish feasibility, impact, human effort, or AI execution cost.
- **adr-scalable-3a-exploration** — Scalable 3A Catalog And Relationship Exploration: The implemented 3A page requests up to 200 projection nodes, renders all returned catalog nodes, chooses an implicit focus, and eagerly loads trace, Alignment, and Drift data. A dense layer stretches the whole lane grid and creates a long page. The trace repository also loads all projection edges before applying an in-memory walk. These behaviors are acceptable only for a small local catalog and contradict the bounded navigation contract of ADR-0022.
- **adr-single-authoritative-local-postgresql** — Single Authoritative Local PostgreSQL: Two independent Docker Compose projects were started from isolated worktrees. The `deploy` PostgreSQL instance contains deployment-validation design facts, while the `specforge-graph-verify` PostgreSQL instance contains the complete authored asset catalog. Allowing Web, MCP, or graph-projection processes to select either instance makes the same application service appear to have different API, data-model, and relationship inventories.
- **adr-single-host-docker-compose-deployment** — Single-Host Docker Compose Deployment: SpecForge needs a repeatable first deployment profile that can run the Web application and its authoritative PostgreSQL store on one Linux host without exposing the database to the host network. The existing development server and local database processes are not a deployment artifact. The profile must support future replacement with an enterprise PostgreSQL connection string without changing application code or the Web image.
- **adr-transactional-outbox-graph-projection** — Transactional Outbox and Scope-Safe Graph Projection: Relationship mutations need a durable source of truth, an auditable event history, and a replayable path to derived graph views. A graph database must not become an independent authoring source, and a projection for one application service must never read or write a sibling service or a near-prefix scope. The impact-analysis worker also needs a persisted projection checkpoint so analysis results can identify the graph version they used.
- **adr-unified-3a-knowledge-initialization** — Unified 3A Knowledge Initialization And Derived Knowledge Layers: Enterprise initialization must recover business, system, and technical architecture without assuming one company-specific methodology. The same implementation evidence can support several architectural claims, claims may conflict, and organization or analysis profiles may change independently from the underlying facts. A monolithic document or one mutable assertion per concept would erase provenance and make later reconciliation unsafe.
- **adr-webgl-3a-graph-exploration** — WebGL 3A Graph Exploration And Impact Analysis: ADR-0023 deliberately selected React Flow for the first bounded relationship-graph increment and deferred WebGL until measured product need justified a renderer change. The increment proved exact-Scope bounded adjacency, continuation, and stable URL focus, but its card-heavy static layout behaves as a relationship preview rather than a modern graph explorer. The user requires both architecture-topology exploration and integrated impact analysis at a quality closer to GitNexus.
- **adr-webgl-data-model-er-workspace** — WebGL Data Model ER Workspace: The Data Model page previously exposed a flat field table and narrative relationship strings. That representation could not show stable entity and field identities, composite mappings, boundary entities, or a bounded impact path. The browser also needs a read-only view that cannot weaken the MCP-only authoring boundary or application-service isolation.
